name: flutter_axios_example
description: Flutter Axios example application demonstrating build_runner JSON serialization
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_axios:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.7.1
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

